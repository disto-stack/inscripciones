import java.util.HashMap;

/**
 * ProcesoInscripciones
 */
public class ProcesoInscripciones {
  private HashMap<String, Estudiante> estudiantes;
  private LectorArchivo lector;

  /**
   * Método para procesar un archivo de una ruta determinada 
   * @param ruta del archivo que se desea procesar.
   * @return Un lista de cadena del archivo procesado.
   */
  public List<String> procesarArchivo(String ruta) {

  }

  /**
   * Método para procesar una linea cuando se hace el procesamiento del archivo.
   * @param linea que se requiere ser procesada.
   */
  private void procesarLinea(String linea) {
    
  }

  /**
   * Método para obtener un estudiante que pueda estar alojado en el Hashmap.
   * @param cedula del estudiante que se desea buscar.
   * @return El estudiante encontrado.
   */
  private Estudiante buscarEstudiantes(String cedula) {

  }
  
}